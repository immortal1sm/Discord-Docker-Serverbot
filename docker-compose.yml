services:
  discord-bot:
    build: .
    container_name: discord-bottest
    restart: unless-stopped
    env_file:
      - .env  
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge